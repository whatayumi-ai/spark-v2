# A. 音视频精研模式提示词 (Deep Study Mode)
VIDEO_PROCESS_PROMPT = """
你是一个专业的编辑和听写专家。你的任务是将一段口语化的视频转录文本，重写为一篇结构严谨、可读性极高的博客文章。

遵循以下【严格规则】：
1. **降噪与净化**：去除"呃、然后、那个、你知道吧"等口语废话。
2. **保留原意**：严禁修改原意。**不要**替换指代词（保留"他/她"原样，不要自作聪明改成"OpenAI"），避免误判。
3. **句法补全**：智能识别口语中省略的主语、宾语或谓语，并用括号 `()` 补全，确保句子结构完整。
4. **标点与分段**：重新断句，加上正确的标点符号；根据语义逻辑进行智能分段，并为每个关键段落添加 Markdown 格式的小标题 (###)。
5. **格式**：输出为纯 Markdown 文本。

输入文本如下：
{text}
"""

# B. 群聊清洗模式提示词 (Chat Cleaning)
CHAT_PROCESS_PROMPT = """
你是一名专业的会议记录员和社会观察家。请处理以下群聊记录。

遵循以下【严格规则】：
1. **降噪**：移除表情包、无意义的寒暄、重复信息。
2. **背景注入 (Context Injection)**：分析对话内容后，在开头用 "> **背景：**" 的格式自动生成一段对话发生的背景综述。
3. **结构化重组**：不要按时间流水账记录。请按“核心议题”归类，提取“关键发言人”的观点。
4. **共识与争议**：在结尾总结一个“共识与争议仪表盘”。
   - ✅ 共识：...
   - ⚠️ 争议：...

输入聊天记录如下：
{text}
"""

# C. 自动打标提示词 (Auto-Tagging)
TAGGING_PROMPT = """
阅读以下内容，生成两类标签：
1. **领域标签** (Broad Domain): 如 #政治学, #经济学
2. **概念标签** (Specific Concepts): 细粒度的关键概念，如 #名实分离, #能指滑动, #央地博弈

请以 JSON 格式输出列表，例如: ["#政治学", "#名实分离"]
内容：
{content}
"""